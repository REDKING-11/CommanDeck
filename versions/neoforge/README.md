# CommanDeck NeoForge Version

The current NeoForge support is still part of the root project as experimental metadata and an entrypoint alongside the Fabric build.

Relevant files:

* `../../src/main/resources/META-INF/neoforge.mods.toml`
* `../../src/main/java/xyz/redking/commanddeck/platform/neoforge/NeoForgeEntrypoint.java`

If NeoForge needs a fully separate build, create it here and keep common behavior aligned with `../../common/`.

# Upload Artifacts

This folder is for built jars copied from the active builds.

Current useful files after building:

* `CommanDeck-1.2-universal.jar` - root Fabric/NeoForge build
* `CommanDeck-1.2-forge-1.20.1.jar` - requested Forge 1.20.1 build

The jar files are ignored by Git because they can be rebuilt.
